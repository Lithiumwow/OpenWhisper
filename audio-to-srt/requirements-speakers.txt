# Optional extras for --speakers (diarization + interactive naming).
# Install into the existing venv:
#   .\.venv\Scripts\pip.exe install -r requirements-speakers.txt
#
# Then:
# 1. Create a free Hugging Face account
# 2. Accept terms for:
#      https://huggingface.co/pyannote/speaker-diarization-community-1
# 3. Put HF_TOKEN=hf_... in the repo .env (or setx / --hf-token)

torch
torchaudio
pyannote.audio
